#include <bits/stdc++.h>

using namespace std;

// char convert(char name)
// {
//     if (name >= 'a' && name <= 'z')
//     {
//         char ans = name - 'a' + 'A';
//         // return name - 32;
//         return ans;
//     }
// }

// int cntdigit(int n)
// {
//     int cnt = 0;
//     while (n)
//     {
//         cnt++;
//         n /= 10;
//     }
//     return cnt;
// }
// bool armstrong(int n, int digit)
// {
//     int sum = 0;
//     int rem;
//     int x = n;
//     while (x)
//     {
//         rem = x % 10;
//         x /= 10;
//         sum += pow(rem, digit);
//     }
//     return sum == n;
// }
int main()
{

    // char name;
    // cin >> name;
    // cout << convert(name);

    // int n;
    // cin >> n;
    // int digit = cntdigit(n);
    // cout << armstrong(n, digit);

    // int a;
    // cin >> a;
    // int b;
    // cin >> b;
    // int cnt = 0;
    // cnt += min(8 - a, 8 - b);
    // cnt += min(8 - a, b - 1);
    // cnt += min(a - 1, b - 1);
    // cnt += min(a - 1, 8 - b);
    // cout << cnt;

    return 0;
}